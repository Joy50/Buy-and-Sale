<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:title="Search Icon"
        android:icon="@drawable/search"
        android:id="@+id/search_main_menu"
        app:showAsAction="always"/>
    <item android:title="My Cart"
        android:icon="@drawable/cartxml_foreground"
        android:id="@+id/cart_main_menu"
        app:showAsAction="always"/>
    <item android:title="Notification"
        android:icon="@drawable/notification"
        android:id="@+id/notification_main_menu"
        app:showAsAction="always"/>
</menu>